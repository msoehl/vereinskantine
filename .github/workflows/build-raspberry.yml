name: Test Upload Artifact

on: [push]

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - name: Create file
        run: echo "Hello Raspberry Pi!" > hello.txt

      - name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: hello-file
          path: hello.txt
